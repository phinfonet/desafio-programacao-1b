<h1>Upload Orders</h1>
<%= form_for :order, url: orders_store_from_file_path, html: {multipart: true} do |f| %>
  <%= f.label :file, 'Orders File' %>
  <%= f.file_field :file %> 
  <%= f.submit 'Submit', disable_with: 'Submiting...', class: 'button' %>
<% end %>
